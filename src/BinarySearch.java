

public class BinarySearch {
  public static int BinarySearch(int[] num, int target) {
    int left = 0;
    int right = 1;
    while (left <= right) {
      int mid = left + (right - left) / 2;


    }
  }
}